poll_interval_seconds: 5
servers:
  - name: 广州服务器
    ssh_host: root@<SERVER_A_PUBLIC_IP>
    gateway_port: 18789
    service_name: openclaw-gateway.service
  - name: 雅加达服务器
    ssh_host: root@<SERVER_B_PUBLIC_IP>
    gateway_port: 18789
    service_name: openclaw-gateway.service
sync:
  roots:
    - /root/files
    - /root/.openclaw/workspace
  excludes:
    - "**/.env"
    - "**/credentials/**"
    - "**/openclaw.json"
    - "**/auth-profiles.json"
    - "**/.codex/**"
  allow_delete: false
  ssh_key_path: /ABS/PATH/TO/YOUR/SSH_PRIVATE_KEY
